OpenAI Codex v0.130.0
--------
workdir: /Users/ryanbak/claude/formatbuddy/desktop
model: gpt-5.5
provider: openai
approval: never
sandbox: read-only
reasoning effort: xhigh
reasoning summaries: none
session id: 019e3873-c481-7d32-9147-5fea5f86bb77
--------
user
# Final regression review — FormatBuddy Desktop v0.1.3

You previously reviewed v0.1.2 and flagged two new findings in the PowerShell integrity protection. v0.1.3 addresses both.

- Repo: https://github.com/ryan-formatbuddy/formatbuddy-desktop (private)
- Release: https://github.com/ryan-formatbuddy/formatbuddy-desktop/releases/tag/v0.1.3
- Working tree: `/Users/ryanbak/claude/formatbuddy/desktop`
- Last commit: `b9e0e0f`

## Your v0.1.2 findings → status

1. P2 — expected hash lived in `script.sha256` next to the script (untrusted bundle)
   → fixed: hash is now in `src/shared/ps-script-hash.ts` (auto-generated by `scripts/embed-ps-hash.mjs`), bundled into `app.asar`. The external `resources/powershell/script.sha256` file is removed.
2. P3 — TOCTOU between hash check and PowerShell spawn (script path is re-opened)
   → fixed: `verifyAndStageScript()` now reads + verifies + copies the bytes to a private `os.tmpdir()/formatbuddy-script/script-<uuid>.ps1`, and PowerShell spawns from the staged path. Cleanup in `finally`.

## What I want

Focus this pass on the **new code only** (rest of the codebase was already cleared in your previous two reviews):

- `scripts/embed-ps-hash.mjs` (now emits a TS module, not a hash file)
- `src/shared/ps-script-hash.ts` (auto-generated)
- `src/main/scanner.ts` — the new `verifyAndStageScript`, the `runScan` try/finally, the staged-path spawn
- `tests/scanner.test.ts` — the 5 integrity tests

Specifically check for:
1. Bypass paths for the integrity check now that the expected hash is in TS code
2. Any remaining TOCTOU window (staging dir permissions, race between staging write and spawn open, symlink tricks)
3. Cleanup correctness in `runScan` finally (e.g. when the spawn promise rejects or is aborted)
4. Test correctness — do the 5 new cases actually exercise the behavior they claim?

Severity-rated (CRITICAL / HIGH / MEDIUM / LOW / NIT). For each finding: `file:line — issue` + one-line fix. **If zero new issues, say so explicitly so I can mark v0.1.3 stable.**
2026-05-18T00:19:32.394871Z ERROR rmcp::transport::worker: worker quit with fatal: Transport channel closed, when AuthRequired(AuthRequiredError { www_authenticate_header: "Bearer error=\"invalid_token\", error_description=\"No authorization provided\", resource_metadata=\"https://mcp.vercel.com/.well-known/oauth-protected-resource\"" })
2026-05-18T00:19:32.629512Z ERROR rmcp::transport::worker: worker quit with fatal: Transport channel closed, when AuthRequired(AuthRequiredError { www_authenticate_header: "Bearer error=\"invalid_token\", error_description=\"No authorization provided\", resource_metadata=\"https://mcp.vercel.com/.well-known/oauth-protected-resource\"" })
exec
/bin/zsh -lc 'git status --short && git rev-parse --show-toplevel && git log --oneline -5' in /Users/ryanbak/claude/formatbuddy/desktop
 succeeded in 5894ms:
2026-05-18 09:19:44.995 xcodebuild[62543:41244800]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-05-18 09:19:45.138 xcodebuild[62543:41244798] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-ykZfBBCe' (errno=Operation not permitted)
2026-05-18 09:19:45.667 xcodebuild[62545:41244811]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-05-18 09:19:45.813 xcodebuild[62545:41244810] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
?? logs/codex-review-v3.md
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-UHmkA3my' (errno=Operation not permitted)
2026-05-18 09:19:46.979 xcodebuild[62550:41244861]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-05-18 09:19:47.125 xcodebuild[62550:41244860] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-zsrZvdJk' (errno=Operation not permitted)
2026-05-18 09:19:47.650 xcodebuild[62552:41244873]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-05-18 09:19:47.793 xcodebuild[62552:41244872] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
/Users/ryanbak/claude/formatbuddy/desktop
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-9usbeehL' (errno=Operation not permitted)
2026-05-18 09:19:48.945 xcodebuild[62557:41244905]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-05-18 09:19:49.086 xcodebuild[62557:41244904] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-mO962KTO' (errno=Operation not permitted)
2026-05-18 09:19:49.611 xcodebuild[62559:41244915]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-05-18 09:19:49.753 xcodebuild[62559:41244913] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
b9e0e0f fix: anchor PS integrity hash in app.asar + stage verified bytes (v0.1.3)
c23d45b feat: PowerShell script integrity verification (v0.1.2)
ce83561 fix: apply 13 of 15 findings from Codex code review (v0.1.1)
4074082 build: add multi-size .ico app icon + disable cross-build code signing
c03d19f chore: initial FormatBuddy Desktop scaffold

exec
/bin/zsh -lc 'git diff --stat c23d45b..b9e0e0f && git diff --unified=80 c23d45b..b9e0e0f -- scripts/embed-ps-hash.mjs src/shared/ps-script-hash.ts src/main/scanner.ts tests/scanner.test.ts' in /Users/ryanbak/claude/formatbuddy/desktop
 succeeded in 3925ms:
2026-05-18 09:19:55.630 xcodebuild[62565:41245008]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-05-18 09:19:55.772 xcodebuild[62565:41245007] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-smMeUrjb' (errno=Operation not permitted)
2026-05-18 09:19:56.299 xcodebuild[62567:41245022]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-05-18 09:19:56.443 xcodebuild[62567:41245021] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
 .gitignore                   |    1 -
 logs/codex-review-v2.md      | 5627 ++++++++++++++++++++++++++++++++++++++++++
 package.json                 |    2 +-
 scripts/embed-ps-hash.mjs    |   34 +-
 src/main/scanner.ts          |   90 +-
 src/shared/ps-script-hash.ts |    8 +
 tests/scanner.test.ts        |   80 +-
 7 files changed, 5770 insertions(+), 72 deletions(-)
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-xm9gcSxM' (errno=Operation not permitted)
2026-05-18 09:19:57.612 xcodebuild[62571:41245065]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-05-18 09:19:57.753 xcodebuild[62571:41245063] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-VVskG8QH' (errno=Operation not permitted)
2026-05-18 09:19:58.276 xcodebuild[62573:41245076]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-05-18 09:19:58.417 xcodebuild[62573:41245075] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
diff --git a/scripts/embed-ps-hash.mjs b/scripts/embed-ps-hash.mjs
index ef81754..52281aa 100644
--- a/scripts/embed-ps-hash.mjs
+++ b/scripts/embed-ps-hash.mjs
@@ -1,24 +1,42 @@
 #!/usr/bin/env node
 /**
- * Compute SHA-256 of the PowerShell scan script and write it to
- * resources/powershell/script.sha256 so the packaged main process can
- * compare it at runtime before spawning.
+ * Compute SHA-256 of the PowerShell scan script and emit it as a TypeScript
+ * constant under src/shared/ps-script-hash.ts.
  *
- * Runs as a `prebuild` and `predist:win` hook. Re-running is safe.
+ * This file is bundled into app.asar at build time so the expected digest
+ * lives inside trusted main-process code, not an external manifest that an
+ * attacker could rewrite alongside the script itself.
+ *
+ * Wired as `prebuild`, `predist:win`, and inside `dev` / `test` scripts.
+ * Re-running is safe and produces deterministic output (only the hex digest
+ * differs between PowerShell revisions).
  */
 import { createHash } from "node:crypto";
-import { readFileSync, writeFileSync } from "node:fs";
+import { mkdirSync, readFileSync, writeFileSync } from "node:fs";
 import { dirname, join, resolve } from "node:path";
 import { fileURLToPath } from "node:url";
 
 const __dirname = dirname(fileURLToPath(import.meta.url));
 const projectRoot = resolve(__dirname, "..");
 const scriptPath = join(projectRoot, "resources", "powershell", "Invoke-FormatBuddyScan.ps1");
-const hashPath = join(projectRoot, "resources", "powershell", "script.sha256");
+const outPath = join(projectRoot, "src", "shared", "ps-script-hash.ts");
 
 const contents = readFileSync(scriptPath);
 const hash = createHash("sha256").update(contents).digest("hex");
 
-writeFileSync(hashPath, `${hash}\n`, "utf8");
+const body = `// AUTO-GENERATED by scripts/embed-ps-hash.mjs — do not edit by hand.
+// Run \`npm run embed-ps-hash\` (or any build/test/dev script) to refresh.
+//
+// SHA-256 of resources/powershell/Invoke-FormatBuddyScan.ps1, anchored in
+// the bundled main code so an attacker cannot bypass the integrity check
+// by editing an external manifest alongside the script.
+
+export const EXPECTED_PS_SCRIPT_HASH = "${hash}";
+`;
+
+mkdirSync(dirname(outPath), { recursive: true });
+writeFileSync(outPath, body, "utf8");
 
-console.info(`[embed-ps-hash] ${scriptPath} → sha256 ${hash}`);
+console.info(`[embed-ps-hash] ${scriptPath}`);
+console.info(`[embed-ps-hash]   → sha256 ${hash}`);
+console.info(`[embed-ps-hash]   → ${outPath}`);
diff --git a/src/main/scanner.ts b/src/main/scanner.ts
index 4d4f21f..36a445a 100644
--- a/src/main/scanner.ts
+++ b/src/main/scanner.ts
@@ -1,210 +1,242 @@
 import { spawn } from "node:child_process";
 import { existsSync, mkdirSync, promises as fs } from "node:fs";
 import { tmpdir } from "node:os";
 import { dirname, join } from "node:path";
 import { createHash, randomUUID } from "node:crypto";
 import type { ScanProgress, ScanReport, ScanResult, ScanStepView } from "@shared/types";
+import { EXPECTED_PS_SCRIPT_HASH } from "@shared/ps-script-hash";
 
 const STDERR_MAX_BYTES = 64 * 1024;
-const INTEGRITY_MANIFEST = "script.sha256";
 
-async function verifyScriptIntegrity(
+/**
+ * Read the on-disk PowerShell script, hash it, compare against the bundled
+ * expected digest, and (on match) copy the verified bytes into a private
+ * temp file. The returned path is what the caller MUST spawn — that closes
+ * the TOCTOU window between hash check and PowerShell open.
+ *
+ * Returns the staged path on success. Returns null when the on-disk script
+ * cannot be read OR the hash does not match AND `enforce` is false (dev
+ * workflow): the caller may then fall back to the original path or refuse
+ * to run.
+ */
+async function verifyAndStageScript(
   scriptPath: string,
-  opts: { enforce: boolean }
-): Promise<void> {
-  const manifestPath = join(dirname(scriptPath), INTEGRITY_MANIFEST);
-  let expected: string;
-  try {
-    expected = (await fs.readFile(manifestPath, "utf8")).trim();
-  } catch {
-    if (opts.enforce) {
-      throw new Error(`PowerShell integrity manifest missing: ${manifestPath}`);
-    }
-    return; // dev / mock — silent skip when manifest hasn't been generated
-  }
-  let actual: string;
+  opts: { enforce: boolean; expectedHash?: string }
+): Promise<string | null> {
+  const expected = opts.expectedHash ?? EXPECTED_PS_SCRIPT_HASH;
+
+  let buf: Buffer;
   try {
-    const buf = await fs.readFile(scriptPath);
-    actual = createHash("sha256").update(buf).digest("hex");
+    buf = await fs.readFile(scriptPath);
   } catch (e) {
     if (opts.enforce) throw e;
-    return;
+    return null;
   }
+
+  const actual = createHash("sha256").update(buf).digest("hex");
   if (actual !== expected) {
-    throw new Error(
-      `PowerShell integrity check failed (expected ${expected.slice(0, 12)}…, got ${actual.slice(0, 12)}…)`
+    if (opts.enforce) {
+      throw new Error(
+        `PowerShell integrity check failed (expected ${expected.slice(0, 12)}…, got ${actual.slice(0, 12)}…)`
+      );
+    }
+    console.warn(
+      `[scanner] PowerShell hash mismatch in dev mode — using original path. expected=${expected.slice(0, 12)}… actual=${actual.slice(0, 12)}…`
     );
+    return null;
   }
+
+  // hash matches: stage verified bytes to a private temp file so the path
+  // we spawn is the same bytes we just verified.
+  const stagedDir = join(tmpdir(), "formatbuddy-script");
+  ensureDir(stagedDir);
+  const stagedPath = join(stagedDir, `script-${randomUUID()}.ps1`);
+  await fs.writeFile(stagedPath, buf);
+  return stagedPath;
 }
 
 function isScanReport(value: unknown): value is ScanReport {
   if (!value || typeof value !== "object") return false;
   const r = value as Record<string, unknown>;
   return (
     typeof r.schemaVersion === "string" &&
     typeof r.generatedAt === "string" &&
     Array.isArray(r.disks) &&
     Array.isArray(r.userFolders) &&
     Array.isArray(r.installedApps) &&
     Array.isArray(r.drivers) &&
     Array.isArray(r.printers) &&
     typeof r.system === "object" &&
     typeof r.privacy === "object" &&
     typeof r.checklist === "object"
   );
 }
 
 async function readAndDelete(path: string): Promise<string> {
   const raw = await fs.readFile(path, "utf8");
   await fs.unlink(path).catch(() => {
     // best-effort cleanup; ignore failures so a Windows lock doesn't crash the flow
   });
   return raw;
 }
 
 export interface RunScanOptions {
   scriptPath: string;
   outputDir: string;
   onProgress?: (progress: ScanProgress) => void;
   powershellExe?: string;
   signal?: AbortSignal;
   /** Synthetic mock instead of spawning powershell (for non-Windows dev / tests). */
   mock?: boolean;
   /** Require script.sha256 to exist and match. Set true for packaged production. */
   enforceIntegrity?: boolean;
 }
 
 const PIPELINE_STEPS: readonly string[] = [
   "PC 정보 확인",
   "디스크 살펴보기",
   "사용자 폴더 챙기기",
   "설치 앱 / 드라이버 목록",
   "인증서·Wi-Fi·클라우드",
   "포맷 체크리스트 정리"
 ];
 
 const TOTAL_STEPS = PIPELINE_STEPS.length;
 
 function buildSteps(activeIndex: number): ScanStepView[] {
   return PIPELINE_STEPS.map((name, i) => {
     if (i < activeIndex) return { name, state: "done", detail: "살펴봤어요" };
     if (i === activeIndex) return { name, state: "active", detail: "보고 있어요" };
     return { name, state: "pending", detail: "대기" };
   });
 }
 
 function progressFor(activeIndex: number, startedAt: number, message?: string): ScanProgress {
   const safeIndex = Math.max(0, Math.min(TOTAL_STEPS, activeIndex));
   const score = Math.min(100, Math.round((safeIndex / TOTAL_STEPS) * 100));
   return {
     step: PIPELINE_STEPS[Math.min(safeIndex, TOTAL_STEPS - 1)],
     doneSteps: safeIndex,
     totalSteps: TOTAL_STEPS,
     score,
     elapsedMs: Date.now() - startedAt,
     steps: buildSteps(safeIndex),
     message
   };
 }
 
 export async function runScan(options: RunScanOptions): Promise<ScanResult> {
   const { onProgress, signal, mock, enforceIntegrity } = options;
   const startedAt = Date.now();
 
-  // Integrity check runs even for mock when a manifest is present (catches
-  // accidental script tampering in dev). It only throws when enforced.
-  if (!mock || enforceIntegrity) {
-    await verifyScriptIntegrity(options.scriptPath, { enforce: !!enforceIntegrity });
+  let stagedPath: string | null = null;
+  if (!mock) {
+    stagedPath = await verifyAndStageScript(options.scriptPath, {
+      enforce: !!enforceIntegrity
+    });
   }
+  const effectiveScriptPath = stagedPath ?? options.scriptPath;
 
   const tmpDir = join(tmpdir(), "formatbuddy-scans");
   ensureDir(tmpDir);
   const outPath = join(tmpDir, `report-${randomUUID()}.json`);
 
   onProgress?.(progressFor(0, startedAt, "버디가 살펴볼 준비 중이에요"));
 
-  if (mock || process.platform !== "win32") {
-    return runMockScan({ outPath, startedAt, onProgress, signal });
+  try {
+    if (mock || process.platform !== "win32") {
+      return await runMockScan({ outPath, startedAt, onProgress, signal });
+    }
+    return await runPowershellScan({
+      ...options,
+      scriptPath: effectiveScriptPath,
+      outPath,
+      startedAt
+    });
+  } finally {
+    if (stagedPath) {
+      await fs.unlink(stagedPath).catch(() => {
+        // best-effort: the temp file is in os.tmpdir() and will be reaped
+      });
+    }
   }
-
-  return runPowershellScan({ ...options, outPath, startedAt });
 }
 
 function ensureDir(dir: string) {
   if (!existsSync(dir)) mkdirSync(dir, { recursive: true });
 }
 
 interface InternalRunArgs {
   outPath: string;
   startedAt: number;
   onProgress?: (progress: ScanProgress) => void;
   signal?: AbortSignal;
 }
 
 async function runMockScan(args: InternalRunArgs): Promise<ScanResult> {
   const { outPath, startedAt, onProgress, signal } = args;
 
   for (let i = 1; i <= TOTAL_STEPS; i++) {
     if (signal?.aborted) throw new DOMException("Scan cancelled", "AbortError");
     await delay(380);
     onProgress?.(progressFor(i, startedAt));
   }
 
   const report: ScanReport = buildMockReport();
   ensureDir(dirname(outPath));
   await fs.writeFile(outPath, JSON.stringify(report, null, 2), "utf8");
 
   // Mock pipeline echoes the on-disk path for parity but the file is ephemeral.
   return { report, jsonPath: outPath };
 }
 
 interface PowershellRunArgs extends RunScanOptions {
   outPath: string;
   startedAt: number;
 }
 
 function runPowershellScan(args: PowershellRunArgs): Promise<ScanResult> {
   const { scriptPath, outPath, startedAt, onProgress, signal } = args;
   const exe = args.powershellExe ?? (process.platform === "win32" ? "powershell.exe" : "pwsh");
 
   return new Promise<ScanResult>((resolveScan, rejectScan) => {
     const child = spawn(
       exe,
       ["-NoProfile", "-NonInteractive", "-ExecutionPolicy", "Bypass", "-File", scriptPath, "-OutputPath", outPath],
       { windowsHide: true }
     );
 
     let activeIndex = 0;
     let stderrBuf = "";
     const tick = setInterval(() => {
       if (activeIndex < TOTAL_STEPS) {
         activeIndex += 1;
         onProgress?.(progressFor(activeIndex, startedAt));
       }
     }, 700);
 
     const cleanup = () => {
       clearInterval(tick);
       signal?.removeEventListener("abort", onAbort);
     };
 
     const onAbort = () => {
       child.kill();
       cleanup();
       rejectScan(new DOMException("Scan cancelled", "AbortError"));
     };
 
     if (signal) {
       if (signal.aborted) {
         onAbort();
         return;
       }
       signal.addEventListener("abort", onAbort, { once: true });
     }
 
     child.stderr.on("data", (chunk: Buffer) => {
       stderrBuf += chunk.toString("utf8");
       if (stderrBuf.length > STDERR_MAX_BYTES) {
         stderrBuf = stderrBuf.slice(-STDERR_MAX_BYTES);
       }
     });
@@ -224,81 +256,81 @@ function runPowershellScan(args: PowershellRunArgs): Promise<ScanResult> {
         const raw = await readAndDelete(outPath);
         const parsed: unknown = JSON.parse(raw);
         if (!isScanReport(parsed)) {
           rejectScan(new Error("Diagnostic JSON did not match expected ScanReport schema."));
           return;
         }
         const report = parsed;
         onProgress?.(progressFor(TOTAL_STEPS, startedAt, "살펴보기 끝났어요"));
         resolveScan({ report, jsonPath: outPath });
       } catch (e) {
         rejectScan(e as Error);
       }
     });
   });
 }
 
 function delay(ms: number) {
   return new Promise<void>((res) => setTimeout(res, ms));
 }
 
 function buildMockReport(): ScanReport {
   return {
     schemaVersion: "0.1.0",
     generatedAt: new Date().toISOString(),
     privacy: {
       localOnly: true,
       noPasswordCollection: true,
       noPrivateKeyUpload: true,
       noBrowserPasswordExtraction: true
     },
     system: {
       manufacturer: "Mock",
       model: "DevPreview",
       serialNumberMasked: "***0000",
       osCaption: "Windows 11 Pro (mock)",
       osVersion: "10.0.22631",
       cpu: "Mock CPU",
       memoryGb: 16
     },
     disks: [{ drive: "C:", sizeGb: 476.62, freeGb: 128.41 }],
     userFolders: [
       { name: "Desktop", path: "C:\\Users\\Ryan\\Desktop", exists: true, sizeGb: 0.42 },
       { name: "Documents", path: "C:\\Users\\Ryan\\Documents", exists: true, sizeGb: 3.7 },
       { name: "Downloads", path: "C:\\Users\\Ryan\\Downloads", exists: true, sizeGb: 12.1 }
     ],
     gpu: ["Mock GPU"],
     installedApps: [
       { name: "Chrome", version: "131.0", publisher: "Google" },
       { name: "KakaoTalk", version: "3.x", publisher: "Kakao" }
     ],
     drivers: [],
     printers: [],
     wifiProfiles: ["home", "office"],
     npkiCandidates: [
       { path: "C:\\Users\\Ryan\\AppData\\LocalLow\\NPKI", exists: true },
       { path: "C:\\NPKI", exists: false }
     ],
     bitlocker: [],
     cloudSync: [
       { provider: "OneDrive", path: "C:\\Users\\Ryan\\OneDrive", exists: true },
       { provider: "Google Drive", path: "C:\\Users\\Ryan\\Google Drive", exists: false }
     ],
     browsers: [
       { name: "Chrome", installed: true },
       { name: "Edge", installed: true },
       { name: "Firefox", installed: false },
       { name: "Whale", installed: true }
     ],
     winget: { available: true, note: "winget is available. App export can be added in Phase 2." },
     diagnostics: [],
     checklist: {
       reviewNpkiManually: true,
       exportWifiProfilesManually: true,
       backupDesktopDocumentsDownloads: true,
       verifyCloudSync: true,
       saveReportBeforeFormat: true
     }
   };
 }
 
-export const __testing = { PIPELINE_STEPS, TOTAL_STEPS, buildSteps, progressFor, verifyScriptIntegrity };
+export const __testing = { PIPELINE_STEPS, TOTAL_STEPS, buildSteps, progressFor, verifyAndStageScript };
diff --git a/src/shared/ps-script-hash.ts b/src/shared/ps-script-hash.ts
new file mode 100644
index 0000000..56ac6b6
--- /dev/null
+++ b/src/shared/ps-script-hash.ts
@@ -0,0 +1,8 @@
+// AUTO-GENERATED by scripts/embed-ps-hash.mjs — do not edit by hand.
+// Run `npm run embed-ps-hash` (or any build/test/dev script) to refresh.
+//
+// SHA-256 of resources/powershell/Invoke-FormatBuddyScan.ps1, anchored in
+// the bundled main code so an attacker cannot bypass the integrity check
+// by editing an external manifest alongside the script.
+
+export const EXPECTED_PS_SCRIPT_HASH = "855321de4dd59effb9ec404c609b6b4506fa7ff660b8f99b1bf72e7fcda38a19";
diff --git a/tests/scanner.test.ts b/tests/scanner.test.ts
index 134b1ae..2e208a2 100644
--- a/tests/scanner.test.ts
+++ b/tests/scanner.test.ts
@@ -1,123 +1,137 @@
 import { describe, it, expect } from "vitest";
 import { mkdtempSync, readFileSync, rmSync, writeFileSync } from "node:fs";
 import { tmpdir } from "node:os";
 import { createHash } from "node:crypto";
 import { join } from "node:path";
 import { runScan, __testing } from "../src/main/scanner";
 import type { ScanProgress, ScanReport } from "../src/shared/types";
 
 describe("scanner mock pipeline", () => {
   it("buildSteps marks states correctly", () => {
     const steps = __testing.buildSteps(2);
     expect(steps.length).toBe(__testing.TOTAL_STEPS);
     expect(steps[0].state).toBe("done");
     expect(steps[1].state).toBe("done");
     expect(steps[2].state).toBe("active");
     expect(steps[3].state).toBe("pending");
   });
 
   it("progressFor computes score and elapsed", () => {
     const started = Date.now() - 1234;
     const p = __testing.progressFor(3, started);
     expect(p.totalSteps).toBe(__testing.TOTAL_STEPS);
     expect(p.doneSteps).toBe(3);
     expect(p.score).toBeGreaterThanOrEqual(50);
     expect(p.elapsedMs).toBeGreaterThanOrEqual(1000);
   });
 
   it("runScan(mock) emits progress, completes, writes JSON", async () => {
     const dir = mkdtempSync(join(tmpdir(), "fb-scan-test-"));
     const events: ScanProgress[] = [];
     try {
       const res = await runScan({
         scriptPath: "ignored.ps1",
         outputDir: dir,
         mock: true,
         onProgress: (p) => events.push(p)
       });
       expect(events.length).toBeGreaterThanOrEqual(__testing.TOTAL_STEPS);
       expect(res.report.schemaVersion).toBe("0.1.0");
       expect(res.report.privacy.localOnly).toBe(true);
       const fileText = readFileSync(res.jsonPath, "utf8");
       const parsed = JSON.parse(fileText) as ScanReport;
       expect(parsed.system.osCaption).toContain("Windows");
     } finally {
       rmSync(dir, { recursive: true, force: true });
     }
   });
 
   it("runScan(mock) cancels on abort", async () => {
     const dir = mkdtempSync(join(tmpdir(), "fb-cancel-test-"));
     const controller = new AbortController();
     try {
       const p = runScan({
         scriptPath: "ignored.ps1",
         outputDir: dir,
         mock: true,
         signal: controller.signal
       });
       setTimeout(() => controller.abort(), 50);
       await expect(p).rejects.toThrowError(/cancel/i);
     } finally {
       rmSync(dir, { recursive: true, force: true });
     }
   });
 });
 
-describe("verifyScriptIntegrity", () => {
-  it("silently passes when manifest is missing and enforce=false", async () => {
-    const dir = mkdtempSync(join(tmpdir(), "fb-integ-skip-"));
-    const scriptPath = join(dir, "fake.ps1");
-    writeFileSync(scriptPath, "echo hi", "utf8");
-    try {
-      await expect(
-        __testing.verifyScriptIntegrity(scriptPath, { enforce: false })
-      ).resolves.toBeUndefined();
-    } finally {
-      rmSync(dir, { recursive: true, force: true });
-    }
+describe("verifyAndStageScript", () => {
+  it("returns null when script is missing and enforce=false", async () => {
+    const result = await __testing.verifyAndStageScript("/tmp/does-not-exist.ps1", {
+      enforce: false,
+      expectedHash: "0".repeat(64)
+    });
+    expect(result).toBeNull();
   });
 
-  it("throws when manifest is missing and enforce=true", async () => {
-    const dir = mkdtempSync(join(tmpdir(), "fb-integ-strict-"));
+  it("throws when script is missing and enforce=true", async () => {
+    await expect(
+      __testing.verifyAndStageScript("/tmp/also-does-not-exist.ps1", {
+        enforce: true,
+        expectedHash: "0".repeat(64)
+      })
+    ).rejects.toThrow();
+  });
+
+  it("returns null on hash mismatch when enforce=false (dev)", async () => {
+    const dir = mkdtempSync(join(tmpdir(), "fb-integ-devmiss-"));
     const scriptPath = join(dir, "fake.ps1");
-    writeFileSync(scriptPath, "echo hi", "utf8");
+    writeFileSync(scriptPath, "Get-Process", "utf8");
     try {
-      await expect(
-        __testing.verifyScriptIntegrity(scriptPath, { enforce: true })
-      ).rejects.toThrowError(/integrity manifest missing/i);
+      const result = await __testing.verifyAndStageScript(scriptPath, {
+        enforce: false,
+        expectedHash: "0".repeat(64)
+      });
+      expect(result).toBeNull();
     } finally {
       rmSync(dir, { recursive: true, force: true });
     }
   });
 
-  it("passes when manifest matches script hash", async () => {
-    const dir = mkdtempSync(join(tmpdir(), "fb-integ-ok-"));
+  it("throws on hash mismatch when enforce=true", async () => {
+    const dir = mkdtempSync(join(tmpdir(), "fb-integ-bad-"));
     const scriptPath = join(dir, "fake.ps1");
-    const body = "Get-ChildItem";
-    writeFileSync(scriptPath, body, "utf8");
-    const hash = createHash("sha256").update(body).digest("hex");
-    writeFileSync(join(dir, "script.sha256"), `${hash}\n`, "utf8");
+    writeFileSync(scriptPath, "Get-Process", "utf8");
     try {
       await expect(
-        __testing.verifyScriptIntegrity(scriptPath, { enforce: true })
-      ).resolves.toBeUndefined();
+        __testing.verifyAndStageScript(scriptPath, {
+          enforce: true,
+          expectedHash: "0".repeat(64)
+        })
+      ).rejects.toThrowError(/integrity check failed/i);
     } finally {
       rmSync(dir, { recursive: true, force: true });
     }
   });
 
-  it("throws when manifest does not match script hash", async () => {
-    const dir = mkdtempSync(join(tmpdir(), "fb-integ-bad-"));
+  it("on match returns a private temp path with verified bytes", async () => {
+    const dir = mkdtempSync(join(tmpdir(), "fb-integ-ok-"));
     const scriptPath = join(dir, "fake.ps1");
-    writeFileSync(scriptPath, "Get-Process", "utf8");
-    writeFileSync(join(dir, "script.sha256"), "0".repeat(64) + "\n", "utf8");
+    const body = "Get-ChildItem -Path C:\\";
+    writeFileSync(scriptPath, body, "utf8");
+    const expected = createHash("sha256").update(body).digest("hex");
     try {
-      await expect(
-        __testing.verifyScriptIntegrity(scriptPath, { enforce: true })
-      ).rejects.toThrowError(/integrity check failed/i);
+      const stagedPath = await __testing.verifyAndStageScript(scriptPath, {
+        enforce: true,
+        expectedHash: expected
+      });
+      expect(stagedPath).toBeTypeOf("string");
+      expect(stagedPath).not.toBe(scriptPath);
+      const stagedBody = readFileSync(stagedPath as string, "utf8");
+      expect(stagedBody).toBe(body);
+      // cleanup
+      rmSync(stagedPath as string, { force: true });
     } finally {
       rmSync(dir, { recursive: true, force: true });
     }
   });
 });

exec
/bin/zsh -lc "grep -R \"runScan\\|enforceIntegrity\\|mock\" -n src tests package.json | head -100 && printf '\\n---scanner lines---\\n' && nl -ba src/main/scanner.ts | sed -n '1,180p' && printf '\\n---tests lines---\\n' && nl -ba tests/scanner.test.ts | sed -n '1,180p' && printf '\\n---script lines---\\n' && nl -ba scripts/embed-ps-hash.mjs && printf '\\n---hash lines---\\n' && nl -ba src/shared/ps-script-hash.ts && printf '\\n---package---\\n' && cat package.json" in /Users/ryanbak/claude/formatbuddy/desktop
 succeeded in 240ms:
src/main/index.ts:7:import { runScan } from "./scanner";
src/main/index.ts:72:      const result: ScanResult = await runScan({
src/main/index.ts:77:        enforceIntegrity: app.isPackaged
src/main/scanner.ts:89:  /** Synthetic mock instead of spawning powershell (for non-Windows dev / tests). */
src/main/scanner.ts:90:  mock?: boolean;
src/main/scanner.ts:92:  enforceIntegrity?: boolean;
src/main/scanner.ts:128:export async function runScan(options: RunScanOptions): Promise<ScanResult> {
src/main/scanner.ts:129:  const { onProgress, signal, mock, enforceIntegrity } = options;
src/main/scanner.ts:133:  if (!mock) {
src/main/scanner.ts:135:      enforce: !!enforceIntegrity
src/main/scanner.ts:147:    if (mock || process.platform !== "win32") {
src/main/scanner.ts:290:      osCaption: "Windows 11 Pro (mock)",
tests/scanner.test.ts:6:import { runScan, __testing } from "../src/main/scanner";
tests/scanner.test.ts:9:describe("scanner mock pipeline", () => {
tests/scanner.test.ts:28:  it("runScan(mock) emits progress, completes, writes JSON", async () => {
tests/scanner.test.ts:32:      const res = await runScan({
tests/scanner.test.ts:35:        mock: true,
tests/scanner.test.ts:49:  it("runScan(mock) cancels on abort", async () => {
tests/scanner.test.ts:53:      const p = runScan({
tests/scanner.test.ts:56:        mock: true,

---scanner lines---
     1	import { spawn } from "node:child_process";
     2	import { existsSync, mkdirSync, promises as fs } from "node:fs";
     3	import { tmpdir } from "node:os";
     4	import { dirname, join } from "node:path";
     5	import { createHash, randomUUID } from "node:crypto";
     6	import type { ScanProgress, ScanReport, ScanResult, ScanStepView } from "@shared/types";
     7	import { EXPECTED_PS_SCRIPT_HASH } from "@shared/ps-script-hash";
     8	
     9	const STDERR_MAX_BYTES = 64 * 1024;
    10	
    11	/**
    12	 * Read the on-disk PowerShell script, hash it, compare against the bundled
    13	 * expected digest, and (on match) copy the verified bytes into a private
    14	 * temp file. The returned path is what the caller MUST spawn — that closes
    15	 * the TOCTOU window between hash check and PowerShell open.
    16	 *
    17	 * Returns the staged path on success. Returns null when the on-disk script
    18	 * cannot be read OR the hash does not match AND `enforce` is false (dev
    19	 * workflow): the caller may then fall back to the original path or refuse
    20	 * to run.
    21	 */
    22	async function verifyAndStageScript(
    23	  scriptPath: string,
    24	  opts: { enforce: boolean; expectedHash?: string }
    25	): Promise<string | null> {
    26	  const expected = opts.expectedHash ?? EXPECTED_PS_SCRIPT_HASH;
    27	
    28	  let buf: Buffer;
    29	  try {
    30	    buf = await fs.readFile(scriptPath);
    31	  } catch (e) {
    32	    if (opts.enforce) throw e;
    33	    return null;
    34	  }
    35	
    36	  const actual = createHash("sha256").update(buf).digest("hex");
    37	  if (actual !== expected) {
    38	    if (opts.enforce) {
    39	      throw new Error(
    40	        `PowerShell integrity check failed (expected ${expected.slice(0, 12)}…, got ${actual.slice(0, 12)}…)`
    41	      );
    42	    }
    43	    console.warn(
    44	      `[scanner] PowerShell hash mismatch in dev mode — using original path. expected=${expected.slice(0, 12)}… actual=${actual.slice(0, 12)}…`
    45	    );
    46	    return null;
    47	  }
    48	
    49	  // hash matches: stage verified bytes to a private temp file so the path
    50	  // we spawn is the same bytes we just verified.
    51	  const stagedDir = join(tmpdir(), "formatbuddy-script");
    52	  ensureDir(stagedDir);
    53	  const stagedPath = join(stagedDir, `script-${randomUUID()}.ps1`);
    54	  await fs.writeFile(stagedPath, buf);
    55	  return stagedPath;
    56	}
    57	
    58	function isScanReport(value: unknown): value is ScanReport {
    59	  if (!value || typeof value !== "object") return false;
    60	  const r = value as Record<string, unknown>;
    61	  return (
    62	    typeof r.schemaVersion === "string" &&
    63	    typeof r.generatedAt === "string" &&
    64	    Array.isArray(r.disks) &&
    65	    Array.isArray(r.userFolders) &&
    66	    Array.isArray(r.installedApps) &&
    67	    Array.isArray(r.drivers) &&
    68	    Array.isArray(r.printers) &&
    69	    typeof r.system === "object" &&
    70	    typeof r.privacy === "object" &&
    71	    typeof r.checklist === "object"
    72	  );
    73	}
    74	
    75	async function readAndDelete(path: string): Promise<string> {
    76	  const raw = await fs.readFile(path, "utf8");
    77	  await fs.unlink(path).catch(() => {
    78	    // best-effort cleanup; ignore failures so a Windows lock doesn't crash the flow
    79	  });
    80	  return raw;
    81	}
    82	
    83	export interface RunScanOptions {
    84	  scriptPath: string;
    85	  outputDir: string;
    86	  onProgress?: (progress: ScanProgress) => void;
    87	  powershellExe?: string;
    88	  signal?: AbortSignal;
    89	  /** Synthetic mock instead of spawning powershell (for non-Windows dev / tests). */
    90	  mock?: boolean;
    91	  /** Require script.sha256 to exist and match. Set true for packaged production. */
    92	  enforceIntegrity?: boolean;
    93	}
    94	
    95	const PIPELINE_STEPS: readonly string[] = [
    96	  "PC 정보 확인",
    97	  "디스크 살펴보기",
    98	  "사용자 폴더 챙기기",
    99	  "설치 앱 / 드라이버 목록",
   100	  "인증서·Wi-Fi·클라우드",
   101	  "포맷 체크리스트 정리"
   102	];
   103	
   104	const TOTAL_STEPS = PIPELINE_STEPS.length;
   105	
   106	function buildSteps(activeIndex: number): ScanStepView[] {
   107	  return PIPELINE_STEPS.map((name, i) => {
   108	    if (i < activeIndex) return { name, state: "done", detail: "살펴봤어요" };
   109	    if (i === activeIndex) return { name, state: "active", detail: "보고 있어요" };
   110	    return { name, state: "pending", detail: "대기" };
   111	  });
   112	}
   113	
   114	function progressFor(activeIndex: number, startedAt: number, message?: string): ScanProgress {
   115	  const safeIndex = Math.max(0, Math.min(TOTAL_STEPS, activeIndex));
   116	  const score = Math.min(100, Math.round((safeIndex / TOTAL_STEPS) * 100));
   117	  return {
   118	    step: PIPELINE_STEPS[Math.min(safeIndex, TOTAL_STEPS - 1)],
   119	    doneSteps: safeIndex,
   120	    totalSteps: TOTAL_STEPS,
   121	    score,
   122	    elapsedMs: Date.now() - startedAt,
   123	    steps: buildSteps(safeIndex),
   124	    message
   125	  };
   126	}
   127	
   128	export async function runScan(options: RunScanOptions): Promise<ScanResult> {
   129	  const { onProgress, signal, mock, enforceIntegrity } = options;
   130	  const startedAt = Date.now();
   131	
   132	  let stagedPath: string | null = null;
   133	  if (!mock) {
   134	    stagedPath = await verifyAndStageScript(options.scriptPath, {
   135	      enforce: !!enforceIntegrity
   136	    });
   137	  }
   138	  const effectiveScriptPath = stagedPath ?? options.scriptPath;
   139	
   140	  const tmpDir = join(tmpdir(), "formatbuddy-scans");
   141	  ensureDir(tmpDir);
   142	  const outPath = join(tmpDir, `report-${randomUUID()}.json`);
   143	
   144	  onProgress?.(progressFor(0, startedAt, "버디가 살펴볼 준비 중이에요"));
   145	
   146	  try {
   147	    if (mock || process.platform !== "win32") {
   148	      return await runMockScan({ outPath, startedAt, onProgress, signal });
   149	    }
   150	    return await runPowershellScan({
   151	      ...options,
   152	      scriptPath: effectiveScriptPath,
   153	      outPath,
   154	      startedAt
   155	    });
   156	  } finally {
   157	    if (stagedPath) {
   158	      await fs.unlink(stagedPath).catch(() => {
   159	        // best-effort: the temp file is in os.tmpdir() and will be reaped
   160	      });
   161	    }
   162	  }
   163	}
   164	
   165	function ensureDir(dir: string) {
   166	  if (!existsSync(dir)) mkdirSync(dir, { recursive: true });
   167	}
   168	
   169	interface InternalRunArgs {
   170	  outPath: string;
   171	  startedAt: number;
   172	  onProgress?: (progress: ScanProgress) => void;
   173	  signal?: AbortSignal;
   174	}
   175	
   176	async function runMockScan(args: InternalRunArgs): Promise<ScanResult> {
   177	  const { outPath, startedAt, onProgress, signal } = args;
   178	
   179	  for (let i = 1; i <= TOTAL_STEPS; i++) {
   180	    if (signal?.aborted) throw new DOMException("Scan cancelled", "AbortError");

---tests lines---
     1	import { describe, it, expect } from "vitest";
     2	import { mkdtempSync, readFileSync, rmSync, writeFileSync } from "node:fs";
     3	import { tmpdir } from "node:os";
     4	import { createHash } from "node:crypto";
     5	import { join } from "node:path";
     6	import { runScan, __testing } from "../src/main/scanner";
     7	import type { ScanProgress, ScanReport } from "../src/shared/types";
     8	
     9	describe("scanner mock pipeline", () => {
    10	  it("buildSteps marks states correctly", () => {
    11	    const steps = __testing.buildSteps(2);
    12	    expect(steps.length).toBe(__testing.TOTAL_STEPS);
    13	    expect(steps[0].state).toBe("done");
    14	    expect(steps[1].state).toBe("done");
    15	    expect(steps[2].state).toBe("active");
    16	    expect(steps[3].state).toBe("pending");
    17	  });
    18	
    19	  it("progressFor computes score and elapsed", () => {
    20	    const started = Date.now() - 1234;
    21	    const p = __testing.progressFor(3, started);
    22	    expect(p.totalSteps).toBe(__testing.TOTAL_STEPS);
    23	    expect(p.doneSteps).toBe(3);
    24	    expect(p.score).toBeGreaterThanOrEqual(50);
    25	    expect(p.elapsedMs).toBeGreaterThanOrEqual(1000);
    26	  });
    27	
    28	  it("runScan(mock) emits progress, completes, writes JSON", async () => {
    29	    const dir = mkdtempSync(join(tmpdir(), "fb-scan-test-"));
    30	    const events: ScanProgress[] = [];
    31	    try {
    32	      const res = await runScan({
    33	        scriptPath: "ignored.ps1",
    34	        outputDir: dir,
    35	        mock: true,
    36	        onProgress: (p) => events.push(p)
    37	      });
    38	      expect(events.length).toBeGreaterThanOrEqual(__testing.TOTAL_STEPS);
    39	      expect(res.report.schemaVersion).toBe("0.1.0");
    40	      expect(res.report.privacy.localOnly).toBe(true);
    41	      const fileText = readFileSync(res.jsonPath, "utf8");
    42	      const parsed = JSON.parse(fileText) as ScanReport;
    43	      expect(parsed.system.osCaption).toContain("Windows");
    44	    } finally {
    45	      rmSync(dir, { recursive: true, force: true });
    46	    }
    47	  });
    48	
    49	  it("runScan(mock) cancels on abort", async () => {
    50	    const dir = mkdtempSync(join(tmpdir(), "fb-cancel-test-"));
    51	    const controller = new AbortController();
    52	    try {
    53	      const p = runScan({
    54	        scriptPath: "ignored.ps1",
    55	        outputDir: dir,
    56	        mock: true,
    57	        signal: controller.signal
    58	      });
    59	      setTimeout(() => controller.abort(), 50);
    60	      await expect(p).rejects.toThrowError(/cancel/i);
    61	    } finally {
    62	      rmSync(dir, { recursive: true, force: true });
    63	    }
    64	  });
    65	});
    66	
    67	describe("verifyAndStageScript", () => {
    68	  it("returns null when script is missing and enforce=false", async () => {
    69	    const result = await __testing.verifyAndStageScript("/tmp/does-not-exist.ps1", {
    70	      enforce: false,
    71	      expectedHash: "0".repeat(64)
    72	    });
    73	    expect(result).toBeNull();
    74	  });
    75	
    76	  it("throws when script is missing and enforce=true", async () => {
    77	    await expect(
    78	      __testing.verifyAndStageScript("/tmp/also-does-not-exist.ps1", {
    79	        enforce: true,
    80	        expectedHash: "0".repeat(64)
    81	      })
    82	    ).rejects.toThrow();
    83	  });
    84	
    85	  it("returns null on hash mismatch when enforce=false (dev)", async () => {
    86	    const dir = mkdtempSync(join(tmpdir(), "fb-integ-devmiss-"));
    87	    const scriptPath = join(dir, "fake.ps1");
    88	    writeFileSync(scriptPath, "Get-Process", "utf8");
    89	    try {
    90	      const result = await __testing.verifyAndStageScript(scriptPath, {
    91	        enforce: false,
    92	        expectedHash: "0".repeat(64)
    93	      });
    94	      expect(result).toBeNull();
    95	    } finally {
    96	      rmSync(dir, { recursive: true, force: true });
    97	    }
    98	  });
    99	
   100	  it("throws on hash mismatch when enforce=true", async () => {
   101	    const dir = mkdtempSync(join(tmpdir(), "fb-integ-bad-"));
   102	    const scriptPath = join(dir, "fake.ps1");
   103	    writeFileSync(scriptPath, "Get-Process", "utf8");
   104	    try {
   105	      await expect(
   106	        __testing.verifyAndStageScript(scriptPath, {
   107	          enforce: true,
   108	          expectedHash: "0".repeat(64)
   109	        })
   110	      ).rejects.toThrowError(/integrity check failed/i);
   111	    } finally {
   112	      rmSync(dir, { recursive: true, force: true });
   113	    }
   114	  });
   115	
   116	  it("on match returns a private temp path with verified bytes", async () => {
   117	    const dir = mkdtempSync(join(tmpdir(), "fb-integ-ok-"));
   118	    const scriptPath = join(dir, "fake.ps1");
   119	    const body = "Get-ChildItem -Path C:\\";
   120	    writeFileSync(scriptPath, body, "utf8");
   121	    const expected = createHash("sha256").update(body).digest("hex");
   122	    try {
   123	      const stagedPath = await __testing.verifyAndStageScript(scriptPath, {
   124	        enforce: true,
   125	        expectedHash: expected
   126	      });
   127	      expect(stagedPath).toBeTypeOf("string");
   128	      expect(stagedPath).not.toBe(scriptPath);
   129	      const stagedBody = readFileSync(stagedPath as string, "utf8");
   130	      expect(stagedBody).toBe(body);
   131	      // cleanup
   132	      rmSync(stagedPath as string, { force: true });
   133	    } finally {
   134	      rmSync(dir, { recursive: true, force: true });
   135	    }
   136	  });
   137	});

---script lines---
     1	#!/usr/bin/env node
     2	/**
     3	 * Compute SHA-256 of the PowerShell scan script and emit it as a TypeScript
     4	 * constant under src/shared/ps-script-hash.ts.
     5	 *
     6	 * This file is bundled into app.asar at build time so the expected digest
     7	 * lives inside trusted main-process code, not an external manifest that an
     8	 * attacker could rewrite alongside the script itself.
     9	 *
    10	 * Wired as `prebuild`, `predist:win`, and inside `dev` / `test` scripts.
    11	 * Re-running is safe and produces deterministic output (only the hex digest
    12	 * differs between PowerShell revisions).
    13	 */
    14	import { createHash } from "node:crypto";
    15	import { mkdirSync, readFileSync, writeFileSync } from "node:fs";
    16	import { dirname, join, resolve } from "node:path";
    17	import { fileURLToPath } from "node:url";
    18	
    19	const __dirname = dirname(fileURLToPath(import.meta.url));
    20	const projectRoot = resolve(__dirname, "..");
    21	const scriptPath = join(projectRoot, "resources", "powershell", "Invoke-FormatBuddyScan.ps1");
    22	const outPath = join(projectRoot, "src", "shared", "ps-script-hash.ts");
    23	
    24	const contents = readFileSync(scriptPath);
    25	const hash = createHash("sha256").update(contents).digest("hex");
    26	
    27	const body = `// AUTO-GENERATED by scripts/embed-ps-hash.mjs — do not edit by hand.
    28	// Run \`npm run embed-ps-hash\` (or any build/test/dev script) to refresh.
    29	//
    30	// SHA-256 of resources/powershell/Invoke-FormatBuddyScan.ps1, anchored in
    31	// the bundled main code so an attacker cannot bypass the integrity check
    32	// by editing an external manifest alongside the script.
    33	
    34	export const EXPECTED_PS_SCRIPT_HASH = "${hash}";
    35	`;
    36	
    37	mkdirSync(dirname(outPath), { recursive: true });
    38	writeFileSync(outPath, body, "utf8");
    39	
    40	console.info(`[embed-ps-hash] ${scriptPath}`);
    41	console.info(`[embed-ps-hash]   → sha256 ${hash}`);
    42	console.info(`[embed-ps-hash]   → ${outPath}`);

---hash lines---
     1	// AUTO-GENERATED by scripts/embed-ps-hash.mjs — do not edit by hand.
     2	// Run `npm run embed-ps-hash` (or any build/test/dev script) to refresh.
     3	//
     4	// SHA-256 of resources/powershell/Invoke-FormatBuddyScan.ps1, anchored in
     5	// the bundled main code so an attacker cannot bypass the integrity check
     6	// by editing an external manifest alongside the script.
     7	
     8	export const EXPECTED_PS_SCRIPT_HASH = "855321de4dd59effb9ec404c609b6b4506fa7ff660b8f99b1bf72e7fcda38a19";

---package---
{
  "name": "formatbuddy-desktop",
  "version": "0.1.3",
  "private": true,
  "description": "FormatBuddy Desktop — Windows-only PC format companion built on Electron.",
  "author": {
    "name": "FormatBuddy",
    "email": "support@formatbuddy.app"
  },
  "license": "UNLICENSED",
  "main": "out/main/index.js",
  "scripts": {
    "embed-ps-hash": "node scripts/embed-ps-hash.mjs",
    "dev": "npm run embed-ps-hash && electron-vite dev",
    "prebuild": "npm run embed-ps-hash",
    "build": "electron-vite build",
    "preview": "electron-vite preview",
    "typecheck:node": "tsc --noEmit -p tsconfig.node.json --composite false",
    "typecheck:web": "tsc --noEmit -p tsconfig.web.json --composite false",
    "typecheck": "npm run typecheck:node && npm run typecheck:web",
    "lint": "eslint .",
    "test": "npm run embed-ps-hash && vitest run",
    "test:watch": "vitest",
    "predist:win": "npm run embed-ps-hash",
    "dist:win": "electron-vite build && electron-builder --win --publish never"
  },
  "dependencies": {
    "@electron-toolkit/preload": "3.0.1",
    "@electron-toolkit/utils": "3.0.0"
  },
  "devDependencies": {
    "@tailwindcss/vite": "4.0.6",
    "@testing-library/react": "16.1.0",
    "@types/node": "22.10.5",
    "@types/react": "19.0.7",
    "@types/react-dom": "19.0.3",
    "@typescript-eslint/eslint-plugin": "8.20.0",
    "@typescript-eslint/parser": "8.20.0",
    "@vitejs/plugin-react": "4.3.4",
    "electron": "33.3.1",
    "electron-builder": "25.1.8",
    "electron-vite": "2.3.0",
    "eslint": "9.18.0",
    "eslint-plugin-react": "7.37.4",
    "eslint-plugin-react-hooks": "5.1.0",
    "jsdom": "26.0.0",
    "react": "19.0.0",
    "react-dom": "19.0.0",
    "tailwindcss": "4.0.6",
    "typescript": "5.7.3",
    "vite": "5.4.11",
    "vitest": "2.1.8"
  }
}

exec
/bin/zsh -lc "nl -ba src/main/scanner.ts | sed -n '180,280p' && printf '\\n---index---\\n' && nl -ba src/main/index.ts | sed -n '50,90p'" in /Users/ryanbak/claude/formatbuddy/desktop
 succeeded in 0ms:
   180	    if (signal?.aborted) throw new DOMException("Scan cancelled", "AbortError");
   181	    await delay(380);
   182	    onProgress?.(progressFor(i, startedAt));
   183	  }
   184	
   185	  const report: ScanReport = buildMockReport();
   186	  ensureDir(dirname(outPath));
   187	  await fs.writeFile(outPath, JSON.stringify(report, null, 2), "utf8");
   188	
   189	  // Mock pipeline echoes the on-disk path for parity but the file is ephemeral.
   190	  return { report, jsonPath: outPath };
   191	}
   192	
   193	interface PowershellRunArgs extends RunScanOptions {
   194	  outPath: string;
   195	  startedAt: number;
   196	}
   197	
   198	function runPowershellScan(args: PowershellRunArgs): Promise<ScanResult> {
   199	  const { scriptPath, outPath, startedAt, onProgress, signal } = args;
   200	  const exe = args.powershellExe ?? (process.platform === "win32" ? "powershell.exe" : "pwsh");
   201	
   202	  return new Promise<ScanResult>((resolveScan, rejectScan) => {
   203	    const child = spawn(
   204	      exe,
   205	      ["-NoProfile", "-NonInteractive", "-ExecutionPolicy", "Bypass", "-File", scriptPath, "-OutputPath", outPath],
   206	      { windowsHide: true }
   207	    );
   208	
   209	    let activeIndex = 0;
   210	    let stderrBuf = "";
   211	    const tick = setInterval(() => {
   212	      if (activeIndex < TOTAL_STEPS) {
   213	        activeIndex += 1;
   214	        onProgress?.(progressFor(activeIndex, startedAt));
   215	      }
   216	    }, 700);
   217	
   218	    const cleanup = () => {
   219	      clearInterval(tick);
   220	      signal?.removeEventListener("abort", onAbort);
   221	    };
   222	
   223	    const onAbort = () => {
   224	      child.kill();
   225	      cleanup();
   226	      rejectScan(new DOMException("Scan cancelled", "AbortError"));
   227	    };
   228	
   229	    if (signal) {
   230	      if (signal.aborted) {
   231	        onAbort();
   232	        return;
   233	      }
   234	      signal.addEventListener("abort", onAbort, { once: true });
   235	    }
   236	
   237	    child.stderr.on("data", (chunk: Buffer) => {
   238	      stderrBuf += chunk.toString("utf8");
   239	      if (stderrBuf.length > STDERR_MAX_BYTES) {
   240	        stderrBuf = stderrBuf.slice(-STDERR_MAX_BYTES);
   241	      }
   242	    });
   243	
   244	    child.on("error", (err) => {
   245	      cleanup();
   246	      rejectScan(err);
   247	    });
   248	
   249	    child.on("close", async (code) => {
   250	      cleanup();
   251	      if (code !== 0) {
   252	        rejectScan(new Error(`PowerShell exited with code ${code}. stderr: ${stderrBuf.slice(0, 500)}`));
   253	        return;
   254	      }
   255	      try {
   256	        const raw = await readAndDelete(outPath);
   257	        const parsed: unknown = JSON.parse(raw);
   258	        if (!isScanReport(parsed)) {
   259	          rejectScan(new Error("Diagnostic JSON did not match expected ScanReport schema."));
   260	          return;
   261	        }
   262	        const report = parsed;
   263	        onProgress?.(progressFor(TOTAL_STEPS, startedAt, "살펴보기 끝났어요"));
   264	        resolveScan({ report, jsonPath: outPath });
   265	      } catch (e) {
   266	        rejectScan(e as Error);
   267	      }
   268	    });
   269	  });
   270	}
   271	
   272	function delay(ms: number) {
   273	  return new Promise<void>((res) => setTimeout(res, ms));
   274	}
   275	
   276	function buildMockReport(): ScanReport {
   277	  return {
   278	    schemaVersion: "0.1.0",
   279	    generatedAt: new Date().toISOString(),
   280	    privacy: {

---index---
    50	  if (!app.isPackaged && devUrl && DEV_RENDERER_PATTERN.test(devUrl)) {
    51	    void mainWindow.loadURL(devUrl);
    52	  } else {
    53	    void mainWindow.loadFile(join(__dirname, "..", "renderer", "index.html"));
    54	  }
    55	}
    56	
    57	function registerIpc() {
    58	  ipcMain.handle(IpcChannels.appVersion, () => app.getVersion());
    59	
    60	  ipcMain.handle(IpcChannels.scanStart, async (event) => {
    61	    if (activeAbort) activeAbort.abort();
    62	    const controller = new AbortController();
    63	    activeAbort = controller;
    64	    const sender = event.sender;
    65	
    66	    const emit = (progress: ScanProgress) => {
    67	      if (sender.isDestroyed() || controller.signal.aborted) return;
    68	      sender.send(IpcChannels.scanProgress, progress);
    69	    };
    70	
    71	    try {
    72	      const result: ScanResult = await runScan({
    73	        scriptPath: getScanScriptPath(),
    74	        outputDir: getScanOutputDir(),
    75	        signal: controller.signal,
    76	        onProgress: emit,
    77	        enforceIntegrity: app.isPackaged
    78	      });
    79	      if (!sender.isDestroyed() && !controller.signal.aborted) {
    80	        sender.send(IpcChannels.scanComplete, result);
    81	      }
    82	      return result;
    83	    } catch (err) {
    84	      const e = err as Error;
    85	      const isAbort = e.name === "AbortError" || /cancel/i.test(e.message || "");
    86	      const payload: ScanError = {
    87	        message: e.message,
    88	        code: (e as NodeJS.ErrnoException).code ?? undefined,
    89	        detail: e.stack
    90	      };

exec
/bin/zsh -lc "python3 -c \"import hashlib,pathlib,re; h=hashlib.sha256(pathlib.Path('resources/powershell/Invoke-FormatBuddyScan.ps1').read_bytes()).hexdigest(); ts=pathlib.Path('src/shared/ps-script-hash.ts').read_text(); m=re.search(r'\\\"([0-9a-f]{64})\\\"', ts).group(1); print(h); print(m); print(h==m)\"" in /Users/ryanbak/claude/formatbuddy/desktop
 succeeded in 1982ms:
2026-05-18 09:20:52.253 xcodebuild[62600:41245802]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-05-18 09:20:52.394 xcodebuild[62600:41245801] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
python3: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
python3: error: couldn't create cache file '/tmp/xcrun_db-uJ1ljHOz' (errno=Operation not permitted)
2026-05-18 09:20:52.918 xcodebuild[62602:41245812]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-05-18 09:20:53.056 xcodebuild[62602:41245811] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
855321de4dd59effb9ec404c609b6b4506fa7ff660b8f99b1bf72e7fcda38a19
855321de4dd59effb9ec404c609b6b4506fa7ff660b8f99b1bf72e7fcda38a19
True

exec
/bin/zsh -lc "grep -R \"powershell\\|asar\\|extraResources\\|files\" -n electron-builder.yml package.json src scripts | head -100 && find . -maxdepth 2 -name '*builder*' -o -name 'electron-builder*' -o -name 'vite.config*'" in /Users/ryanbak/claude/formatbuddy/desktop
 succeeded in 270ms:
electron-builder.yml:8:files:
electron-builder.yml:15:extraResources:
electron-builder.yml:16:  - from: resources/powershell
electron-builder.yml:17:    to: powershell
electron-builder.yml:46:asar: true
electron-builder.yml:47:asarUnpack:
src/renderer/src/pages/Report.tsx:37:  const wifiCount = report.wifiProfiles.length;
src/shared/types.ts:99:  exportWifiProfilesManually: boolean;
src/shared/types.ts:116:  wifiProfiles: string[];
src/shared/ps-script-hash.ts:4:// SHA-256 of resources/powershell/Invoke-FormatBuddyScan.ps1, anchored in
src/main/paths.ts:7: * Packaged: process.resourcesPath/powershell/Invoke-FormatBuddyScan.ps1
src/main/paths.ts:8: * Dev: <projectRoot>/resources/powershell/Invoke-FormatBuddyScan.ps1
src/main/paths.ts:14:    return join(process.resourcesPath, "powershell", fileName);
src/main/paths.ts:17:  const devPath = resolve(__dirname, "..", "..", "resources", "powershell", fileName);
src/main/paths.ts:20:  return resolve(process.cwd(), "resources", "powershell", fileName);
src/main/scanner.ts:87:  powershellExe?: string;
src/main/scanner.ts:89:  /** Synthetic mock instead of spawning powershell (for non-Windows dev / tests). */
src/main/scanner.ts:200:  const exe = args.powershellExe ?? (process.platform === "win32" ? "powershell.exe" : "pwsh");
src/main/scanner.ts:308:    wifiProfiles: ["home", "office"],
src/main/scanner.ts:328:      exportWifiProfilesManually: true,
scripts/embed-ps-hash.mjs:6: * This file is bundled into app.asar at build time so the expected digest
scripts/embed-ps-hash.mjs:21:const scriptPath = join(projectRoot, "resources", "powershell", "Invoke-FormatBuddyScan.ps1");
scripts/embed-ps-hash.mjs:30:// SHA-256 of resources/powershell/Invoke-FormatBuddyScan.ps1, anchored in
./dist/builder-debug.yml
./node_modules/electron-builder-squirrel-windows
./node_modules/app-builder-lib
./node_modules/xmlbuilder
./node_modules/builder-util
./node_modules/electron-builder
./node_modules/app-builder-bin
./node_modules/builder-util-runtime
./node_modules/dmg-builder
./electron-builder.yml

exec
/bin/zsh -lc 'cat electron-builder.yml && nl -ba src/main/paths.ts' in /Users/ryanbak/claude/formatbuddy/desktop
 succeeded in 0ms:
appId: app.formatbuddy.desktop
productName: FormatBuddy
copyright: Copyright (c) ${author}
directories:
  output: dist
  buildResources: resources

files:
  - out/**/*
  - package.json
  - "!**/.vscode/*"
  - "!**/{.eslintrc.cjs,.eslintrc,.prettierrc,.editorconfig,.gitignore,.npmrc}"
  - "!**/{tsconfig.*,vite.config.*,vitest.config.*,electron-builder.yml,electron.vite.config.*}"

extraResources:
  - from: resources/powershell
    to: powershell
  - from: resources/fonts
    to: fonts
  - from: resources/icons
    to: icons

win:
  target:
    - target: nsis
      arch:
        - x64
  artifactName: ${productName}-Setup-${version}-${arch}.${ext}
  icon: resources/icons/app-icon.ico
  # Phase 1: unsigned. macOS cross-build cannot run Windows signtool.
  # Ryan must accept SmartScreen warning on first launch on Windows.
  signtoolOptions: null
  signAndEditExecutable: false
  verifyUpdateCodeSignature: false

nsis:
  oneClick: false
  perMachine: false
  allowToChangeInstallationDirectory: true
  createDesktopShortcut: true
  shortcutName: FormatBuddy
  installerLanguages:
    - ko_KR
    - en_US

asar: true
asarUnpack:
  - resources/**
     1	import { app } from "electron";
     2	import { existsSync } from "node:fs";
     3	import { join, resolve } from "node:path";
     4	
     5	/**
     6	 * Resolve the PowerShell scan script path.
     7	 * Packaged: process.resourcesPath/powershell/Invoke-FormatBuddyScan.ps1
     8	 * Dev: <projectRoot>/resources/powershell/Invoke-FormatBuddyScan.ps1
     9	 */
    10	export function getScanScriptPath(): string {
    11	  const fileName = "Invoke-FormatBuddyScan.ps1";
    12	
    13	  if (app.isPackaged) {
    14	    return join(process.resourcesPath, "powershell", fileName);
    15	  }
    16	
    17	  const devPath = resolve(__dirname, "..", "..", "resources", "powershell", fileName);
    18	  if (existsSync(devPath)) return devPath;
    19	
    20	  return resolve(process.cwd(), "resources", "powershell", fileName);
    21	}
    22	
    23	export function getScanOutputDir(): string {
    24	  return join(app.getPath("userData"), "scans");
    25	}
    26	
    27	export function getDefaultExportPath(fileName = "formatbuddy-report.json"): string {
    28	  return join(app.getPath("desktop"), fileName);
    29	}
    30	
    31	export function getWebReportImportUrl(): string {
    32	  return "https://formatbuddy.vercel.app/report/import";
    33	}

exec
/bin/zsh -lc 'cat tsconfig.node.json electron.vite.config.ts vitest.config.ts' in /Users/ryanbak/claude/formatbuddy/desktop
 succeeded in 0ms:
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "lib": ["ES2022"],
    "types": ["node", "electron-vite/node"],
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "esModuleInterop": true,
    "resolveJsonModule": true,
    "skipLibCheck": true,
    "isolatedModules": true,
    "composite": true,
    "outDir": "out/types-node",
    "baseUrl": ".",
    "paths": {
      "@shared/*": ["src/shared/*"],
      "@main/*": ["src/main/*"]
    }
  },
  "include": [
    "electron.vite.config.ts",
    "src/main/**/*.ts",
    "src/preload/**/*.ts",
    "src/shared/**/*.ts",
    "tests/**/*.test.ts"
  ]
}
import { resolve } from "node:path";
import { defineConfig, externalizeDepsPlugin } from "electron-vite";
import react from "@vitejs/plugin-react";

export default defineConfig({
  main: {
    plugins: [externalizeDepsPlugin()],
    build: {
      outDir: "out/main",
      rollupOptions: {
        input: { index: resolve(__dirname, "src/main/index.ts") }
      }
    },
    resolve: {
      alias: { "@shared": resolve(__dirname, "src/shared") }
    }
  },
  preload: {
    plugins: [externalizeDepsPlugin()],
    build: {
      outDir: "out/preload",
      rollupOptions: {
        input: { index: resolve(__dirname, "src/preload/index.ts") }
      }
    },
    resolve: {
      alias: { "@shared": resolve(__dirname, "src/shared") }
    }
  },
  renderer: {
    root: resolve(__dirname, "src/renderer"),
    plugins: [react()],
    build: {
      outDir: resolve(__dirname, "out/renderer"),
      rollupOptions: {
        input: { index: resolve(__dirname, "src/renderer/index.html") }
      }
    },
    resolve: {
      alias: {
        "@renderer": resolve(__dirname, "src/renderer/src"),
        "@shared": resolve(__dirname, "src/shared")
      }
    },
    server: {
      port: 5273
    }
  }
});
import { defineConfig } from "vitest/config";
import { resolve } from "node:path";

export default defineConfig({
  test: {
    environment: "node",
    include: ["tests/**/*.test.ts"],
    globals: false,
    coverage: { reporter: ["text"] }
  },
  resolve: {
    alias: {
      "@shared": resolve(__dirname, "src/shared"),
      "@main": resolve(__dirname, "src/main")
    }
  }
});

codex
The expected hash is now anchored in TypeScript/app code, but the staged script is still written to a predictable non-verified temp directory. That leaves a remaining TOCTOU-style bypass of the new integrity protection.

Review comment:

- [P2] Create the staged script in a private temp directory — /Users/ryanbak/claude/formatbuddy/desktop/src/main/scanner.ts:51-54
  When `os.tmpdir()` is shared or `formatbuddy-script` already exists with attacker-writable ACLs or as a symlink, `ensureDir` accepts that predictable location and `writeFile` emits the verified script there with default permissions. Because PowerShell later reopens this path, an attacker who can watch/modify that directory can replace `script-<uuid>.ps1` after staging and before spawn, bypassing the bundled hash. Use a fresh per-run `fs.mkdtemp` directory with restrictive permissions, write the file with exclusive creation/restrictive mode, reject symlinked parents, and remove the directory after use.
The expected hash is now anchored in TypeScript/app code, but the staged script is still written to a predictable non-verified temp directory. That leaves a remaining TOCTOU-style bypass of the new integrity protection.

Review comment:

- [P2] Create the staged script in a private temp directory — /Users/ryanbak/claude/formatbuddy/desktop/src/main/scanner.ts:51-54
  When `os.tmpdir()` is shared or `formatbuddy-script` already exists with attacker-writable ACLs or as a symlink, `ensureDir` accepts that predictable location and `writeFile` emits the verified script there with default permissions. Because PowerShell later reopens this path, an attacker who can watch/modify that directory can replace `script-<uuid>.ps1` after staging and before spawn, bypassing the bundled hash. Use a fresh per-run `fs.mkdtemp` directory with restrictive permissions, write the file with exclusive creation/restrictive mode, reject symlinked parents, and remove the directory after use.
